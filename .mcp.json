{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--config",
        "./.playwright-mcp-config.json",
        "--isolated"
      ]
    }
  }
}